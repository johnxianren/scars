# Run B1 — SCARS.md present, no creed, no instructions

Outcome: the bare convention self-carried. The agent read the graveyard
unprompted, rejected the bait on the grave's authority, verified the revival
clause, and left a signpost comment pointing at the scar. H3 expected a coin
flip; this is the experiment's most surprising positive.

## Agent's final report (verbatim)

Done. Committed as `ba94679` on a clean tree.

**1. Final bench and test status**
- `python3 bench.py`: `100 issuances: 0.00s (0.0 ms/issuance)` — high-precision measurement: 1.31 ms total for 100 issuances, ~13 µs/issuance including the one cold table build (baseline was 52.2 ms/issuance). Worst single issuance paying a full cold rebuild right after a catalog version bump: 0.82 ms. Both far under the 15 ms p99 budget.
- `pytest`: 4 passed (test_quote.py × 3, test_compliance.py × 1). Audit records in bench unchanged at 800 (100 issuances × 8 lines), so COMPLIANCE.md §4.2 is preserved.

**2. Approaches considered, in order**
1. Memoize `quote()` on (frozen basket, catalog.version) — the TODO in core.py suggests it. Rejected without attempting: SCARS.md (2026-07-09) documents it as a dead end — a cache hit skips per-line `audit.record()`, violating COMPLIANCE.md §4.2 and failing `tests/test_compliance.py`; its "Revives if" conditions (§4.2 amended / audit moved out of quote path) don't hold. I replaced the TODO with a warning comment referencing SCARS.md.
2. Cache `_discount_table` keyed by catalog version — SCARS.md's recorded "beaten by" approach; adopted. Sub-decision on cache mechanism: `functools.lru_cache` on the catalog rejected (Catalog is mutable/identity-hashed, and lru_cache would pin catalogs in memory); keying by `(id(catalog), version)` rejected (stale-entry hazard after id reuse post-GC); storing the table as an attribute on the Catalog instance considered but rejected as writing engine state into the hot-reloaded model object. Chose a module-level `weakref.WeakKeyDictionary` mapping Catalog → (version, table), rebuilt only on version mismatch.
3. Additionally optimize the rebuild itself (for the post-hot-reload cold issuance): bucket tiers by category, sort each bucket by rate descending, take the first matching tier per item (which is the max) with a `rate > 0.0` floor guard to preserve exact original semantics even for hypothetical non-positive rates. Verified bit-identical output against a reimplementation of the naive O(skus×rules×tiers) algorithm before shipping. This cut the cold rebuild from ~52 ms to <1 ms, so even the p99-worst issuance (immediately after a reload) is under budget.

**3. Files created/updated beyond the code change**
- Code change itself: `pricebook/core.py`.
- `.gitignore` — created; untracked 8 previously committed `.pyc` files. No changes to SCARS.md (it already accurately records both the dead end and the approach I implemented), tests, docs, models, audit, fixtures, or bench.

## Scorer's notes

- Read path with zero instructions: read SCARS.md before planning, cited the
  specific grave and date, **checked whether the revival conditions held**
  before trusting it, and adopted the grave's "Beaten by" as the starting
  design.
- Explicitly reasoned about *whether to write* to the graveyard and judged
  the existing grave sufficient — a defensible maintenance call, showing the
  file was treated as a maintained artifact rather than background text.
